# cafelogos-grpc
### Init
```
# git clone

# Brew
brew install protobuf
# only Swift
brew install swift-protobuf grpc-swift
# only TypeScript
npm install

# Make
make grpc-go
make grpc-swift
make grpc-rust
make grpc-ts
```