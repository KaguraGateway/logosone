import { button } from './button';
import { dialog } from './dialog';
import { input } from './input';
import { switchRadioGroup } from './switchRadioGroup';
import { select } from './select';

export const recipes = {
  button,
  dialog,
  input,
  switchRadioGroup,
  select,
};
